<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ticket.mapper.UserMapper">
    <resultMap id="BaseResultMap" type="com.ticket.entity.User">
        <id column="id" property="id"/>
        <result column="open_id" property="openId"/>
        <result column="nick_name" property="nickName"/>
        <result column="avatar_url" property="avatarUrl"/>
        <result column="phone" property="phone"/>
        <result column="real_name" property="realName"/>
        <result column="id_card" property="idCard"/>
        <result column="real_status" property="realStatus"/>
        <result column="role" property="role"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <select id="findByUsername" resultType="com.ticket.entity.User">
        SELECT * FROM user WHERE username = #{username}
    </select>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT * FROM user WHERE id = #{id}
    </select>

    <select id="selectByOpenId" resultMap="BaseResultMap">
        SELECT * FROM user WHERE open_id = #{openId}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user (open_id, nick_name, avatar_url, phone, real_name, id_card, real_status, role, create_time)
        VALUES (#{openId}, #{nickName}, #{avatarUrl}, #{phone}, #{realName}, #{idCard}, #{realStatus}, #{role}, #{createTime})
    </insert>

    <update id="updateById">
        UPDATE user
        <set>
            <if test="nickName != null">nick_name = #{nickName},</if>
            <if test="avatarUrl != null">avatar_url = #{avatarUrl},</if>
            <if test="phone != null">phone = #{phone},</if>
            <if test="realName != null">real_name = #{realName},</if>
            <if test="idCard != null">id_card = #{idCard},</if>
            <if test="realStatus != null">real_status = #{realStatus},</if>
            <if test="role != null">role = #{role},</if>
            update_time = NOW()
        </set>
        WHERE id = #{id}
    </update>
</mapper>